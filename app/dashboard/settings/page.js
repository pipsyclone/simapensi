import Settings from "@/components/settings";

export const metadata = {
	title: "Settings",
	description: "User Settings Account",
};

export default function SettingsPage() {
	return <Settings />;
}
